1. Se accede a la carpeta por medio de la terminal

2. npm install , para instalar las dependencias

3. Se ingresa a la subcarpeta de api  : cd api

4. Dentro de la carpeta api se levanta el backend usando la instrucción node index.js

5. Se abre el archivo index.html dentro de la carpeta public y se accede a la página para su utilización
